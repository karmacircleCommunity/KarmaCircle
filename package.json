{
  "name": "karmacircle",
  "private": true,
  "description": "Monorepo for the KarmaCircle (Milan) platform: apps/web (frontend) and apps/api (backend).",
  "license": "MIT",
  "packageManager": "pnpm@11.22.0",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "type-check": "turbo run type-check",
    "test": "turbo run test"
  },
  "devDependencies": {
    "turbo": "^2.10.11"
  }
}
