version: "3.8"

services:
  app:
    restart: always
    container_name: karmacircle-frontend-container
    image: karmacircle-frontend
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./src:/app/src
    ports:
      - 3000:3000
    env_file:
      - .env